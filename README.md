# `@givemefish/prettier-config`

> My [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ npm i -D @givemefish/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@givemefish/prettier-config"
}
```
